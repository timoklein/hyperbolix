name: Documentation

on:
  push:
    branches: [main, documentation]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for mike versioning

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --locked --dev

      - name: Build documentation
        run: uv run mkdocs build --strict

      - name: Upload documentation artifact
        uses: actions/upload-artifact@v4
        with:
          name: documentation-site
          path: site/
          retention-days: 30

      # Deployment step - DISABLED until repo is public
      # Uncomment the following steps when repo moves to public organization

      # - name: Deploy to GitHub Pages (main branch only)
      #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      #   run: |
      #     git config user.name github-actions
      #     git config user.email github-actions@github.com
      #     uv run mike deploy --push --update-aliases latest main
